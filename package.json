{
  "scripts": {
    "start": "node index.js",
    "build": "tsc -p .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "add": "^2.0.6",
    "typescript": "^5.6.2",
    "yarn": "^1.22.22"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1691.0"
  }
}
